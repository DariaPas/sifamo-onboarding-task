server:
  port: 8081
spring:
  application:
    name: customer-service
  datasource:
    url: jdbc:postgresql://localhost:5432/sifamo_onboarding
    username: customer_app
    password: customer_password

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: customer_schema

